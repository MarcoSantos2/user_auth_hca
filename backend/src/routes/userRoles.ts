// import { Router } from 'express';
// import { assignRole, getUserRoles, removeUserRole } from '../controllers/userController';

// const router = Router();

// router.post('/', assignRole);
// router.get('/:userId', getUserRoles);
// router.delete('/:id', removeUserRole);

// export default router;